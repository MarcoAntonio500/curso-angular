<div class="form-group row">
  <div class="form-group col-md-5">
    <label for="bairro" class="control-label">Bairro</label>
    <input type="text" class="form-control" id="bairro"
      [class.is-invalid]="!bairro.valid && bairro.touched" name="bairro"
      ngModel required="" #bairro="ngModel">
      <div *ngIf="!bairro.valid && bairro.touched"
        class="alert alert-danger" role="alert">
        Bairro é <strong>OBRIGATÓRIO</strong>
      </div>
  </div>

    <div class="form-group col-md-4">
      <label for="cidade" class="control-label">Cidade</label>
      <input type="text" class="form-control" id="cidade"
        [class.is-invalid]="!cidade.valid && cidade.touched" name="cidade"
        ngModel required="" #cidade="ngModel">
        <div *ngIf="!cidade.valid && cidade.touched"
          class="alert alert-danger" role="alert">
          Cidade é <strong>OBRIGATÓRIO</strong>
        </div>
  </div>


    <div class="form-group col-md-3">
      <label for="estado" class="control-label">Estado</label>
      <input type="text" class="form-control" id="estado"
        [class.is-invalid]="!estado.valid && estado.touched" name="estado"
        ngModel required="" #estado="ngModel">
        <div *ngIf="!estado.valid && estado.touched"
          class="alert alert-danger" role="alert">
          Estado é <strong>OBRIGATÓRIO</strong>
        </div>
    </div>
</div>
